<% var show_posts = page.posts.sort('date', 'desc') %>

<% show_posts.each(function(post, i){ %>
	<%- partial('post', { page: post, class_name: 'meta' }) %>
<% }); %>



<%= page.tag %>
<%= page.category %>

<%- partial('archive', {pagination: config.tag, index: true, foo: 333432}) %>
